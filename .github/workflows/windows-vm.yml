name: Windows VM Runner

on:
  workflow_dispatch:

jobs:
  windows-vm:
    runs-on: windows-latest
    timeout-minutes: 360
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Windows VM
      run: |
        echo "🎉 Your FREE Windows VM is running!"
        echo "📍 VM Type: Windows Server 2022"
        echo "⏰ Time Remaining: 6 hours"
        echo "💻 CPU: 2-core, RAM: 7GB, Storage: 14GB"
        echo "🌐 Internet: Available"
        echo "📁 Your files are available in this repository"
        
    - name: Download Chrome manually
      run: |
        # Chrome direct download karein
        Invoke-WebRequest -Uri "https://dl.google.com/chrome/install/latest/chrome_installer.exe" -OutFile "chrome_installer.exe"
        Start-Process -FilePath "chrome_installer.exe" -ArgumentList "/silent", "/install" -Wait
        
    - name: Keep VM running
      run: |
        echo "🚀 VM is ready to use!"
        echo "📝 You can run commands in this workflow"
        echo "⏰ VM will auto-stop after 6 hours"
        # Keep running for 6 hours
        Start-Sleep -Seconds 21600
